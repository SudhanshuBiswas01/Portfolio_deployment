# Drop your generated assets here

Place these files in `/public` (same names, referenced directly by the site):

| File | Used by | Notes |
|------|---------|-------|
| `hero-neural-network.mp4` | Hero (ScrollExpandMedia) | 8s loop, blue/violet neural net on black. Compress before shipping (see README). |
| `hero-poster.jpg` | Hero poster frame | First frame of the video; shown while it loads. |
| `education-collage.jpg` | Education / "The path" background | 3-panel IIT GN / IU Germany / LSBU duotone. |
| `og.png` | SEO / social preview | 1200x630 Open Graph image. |

Until the real files are added, the hero video area will simply show its
poster / empty frame and the education background layer will be blank —
nothing breaks.
